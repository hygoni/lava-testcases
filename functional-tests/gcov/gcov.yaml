metadata:
  format: LAVA test definition 1.0
  name: gcov
  description: "Collect GCOV files for code coverage data. If not configured, just skip."
  maintainer:
    - 42.hyeyoo@gmail.com

run:
  steps:
    - /bin/name.sh $TREE_NAME
    - /bin/coverage.sh $TREE_NAME $GCOV_FILE_NAME
